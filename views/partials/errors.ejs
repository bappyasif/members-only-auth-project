<% if(errors) { %>
    <div class="errors">
        <p>errors list</p>
        <ul>
            <% errors.forEach(err => { %>
                <li><%= err.msg %></li>
            <% }) %>
        </ul>
    </div>
<% } %>